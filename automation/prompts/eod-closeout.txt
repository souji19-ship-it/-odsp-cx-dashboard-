ODSP-AW EOD close-out (daily 1:00 AM CST) - run the routine IN THIS ORDER: (A) UPDATE THE COMMAND CENTER FULLY, section by section, verified against source; (B) CLEAN CODE; (C) CLEAN REPO + commit; then reconcile + finalize. Work in C:\Repo\OneDrive-SharePoint-in-Agentic-Work\Data-Private\Data-Created\ODSP-AW-CC.html. (1) [Step A] Review + UPDATE EVERY Command Center tab for accuracy as the EOD sweep - correct anything stale so the whole cockpit is honest each morning. Tab by tab: TRACKER (board statuses honest, the Daily Effort Ticker + What I Need From You current); DASHBOARDS / Hub; INSIGHTS (the verified Q&A + insight cards, each dated honestly to its source); DATA HEALTH & AUDIT (Data Inventory with two live timestamps per row - data as-of + pulled, Data Classification, Audit Report); OPERATIONS playbook (Governance Principles, Standing Asks, automation cards, My Daily Workload, dashboard/Pulse cadences, Memory); STORAGE & ARCHIVE (footprint + backup/resilience status; Activity Log, Decisions, Audit History, effort log all current). Ensure every tab reflects ALL of Ambal's current durable asks; add any missing one. Keep these honest across the cockpit: the morning memo is the single daily artifact; the debug Edge is CDP 9222; the morning memo + EOD close-out + weekly reflection now run as Windows Scheduled Tasks (not CLI schedules); the CoWork scraper self-heals (dashboard-SPA warm-up + on-401 retry, no manual re-auth); Data-Public keeps a per-night provenance manifest only; the Command Center lives off git under Data-Private/Data-Created (backed by the OneDrive mirror + the off-OneDrive backup), and Data-Private grounding lives in Data-Private/Data-Given/ (off-OneDrive backup via scripts/backup-data-private.ps1); the current tabs are Tracker / Insights / Dashboards / Data Health / Operations / Storage & Archive. NEVER fabricate - if a value cannot be verified, mark it unverified rather than inventing it. RE-VERIFY the entire DATA HEALTH & AUDIT tab section by section: the Data Inventory by Surface (every row's TWO timestamps - data as-of + pulled - are re-read live from the source files by scripts/stamp-data-health.js, never hand-set), the Data Classification, and the Audit Report (re-verify each check against source and correct any stale row); ALSO append a fresh dated row to the Audit History (now in the ARCHIVE tab, auditHistory) so it is never stuck in time - if no new data was verified that day, say so honestly in the note rather than inventing a pass. (2) Conflict check: scan Standing Asks + Governance for contradictions; for EACH conflict, prepend a row to the active-attention tbody (id=active-attention) for Ambal. (3) Memory hygiene: review the Operations Memory section and raise any personal preference that has crept into agent memory - keep Command Center memory and agent memory separate. (4) The End-of-Day Partnership Review is now folded into the next morning's memo (per Ambal) - do NOT generate a separate partnership-review page or a separate EOD memo. Instead FINALIZE the day so the 8:15am morning memo can produce the review for the day just ended: close + verify today's effortTicker row (Operations / Data Wrangling / Insights split + prompt count), and make sure the progressBoard + Decisions + Activity logs are current and accurate. (4c) [Steps B + C] CLEAN CODE, then CLEAN REPO (persisted - in this order, AFTER the full CC update in step A): (B) prune any dead or orphaned code, scripts, and temp/scratch files + remove duplication; (C) ensure the working tree is tidy + review-ready with nothing stray left uncommitted, validate-cc + smoke-cc must pass, then commit. (5) Then run, in PowerShell: & 'C:\Repo\OneDrive-SharePoint-in-Agentic-Work\scripts\cc-maintenance.ps1' (validate + rebuild indexes/memo/archives), THEN & 'C:\Repo\OneDrive-SharePoint-in-Agentic-Work\scripts\archive-eod.ps1' (the flat archive - the close-out runs it directly each night at 1 AM; a separate ODSP-AW-EOD-Archive task at 1:45 AM is a backstop). Do NOT open a browser. Keep it silent and concise.
