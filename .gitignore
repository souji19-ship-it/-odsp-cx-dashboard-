# Data-Private — human-provided / sensitive source materials (recaps, notes, decks, grounding screenshots). Never read by the Agent or LT Dashboard. OneDrive-backed-up.
Data-Private/
# Data-Public — scraped/ingested telemetry (large, regenerated weekly). Agent / LT Dashboard / Ops safe. OneDrive-backed-up.
Data-Public/

# Node / build artifacts
node_modules/
*.log

# Autonomous agent run logs (headless cadence tasks) — local only
automation/agent-logs/

# OS files
Thumbs.db
.DS_Store
desktop.ini

# Editor
.vscode/
*.swp
*.swo
scripts/refresh-log.txt

# Source / working decks (PowerPoint) — internal, often sensitivity-labeled. Keep out of GitHub; the committed deliverables are the HTML cockpit + dashboards.
*.pptx
*.ppt
